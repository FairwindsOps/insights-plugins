apiVersion: v1
kind: ConfigMap
metadata:
  name: network-flow
  namespace: insights
data:
  config.yaml: |-
    events-buffer-length: 32768
    gadget-namespace: insights
    daemon-log-level: info
    operator:
      kubemanager:
        hook-mode: auto
