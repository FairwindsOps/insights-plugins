apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: network-flow
spec:
  template:
    spec:
      containers:
        - name: agent
          env:
            - name: COLLECTOR_ADDR
              value: "127.0.0.1:1"
            - name: MAX_PENDING_EVENTS
              value: "1000"
