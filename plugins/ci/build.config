PLUGIN_NAME='ci'
REPOSITORY_NAME=fairwinds/insights-ci
RUN_TEST=false
. `dirname "$BASH_SOURCE"`/../base.config
BASEDIR='./plugins' # Set BASEDIR up one level so that we can pull the Trivy code in
DOCKERFILE='ci/Dockerfile'
