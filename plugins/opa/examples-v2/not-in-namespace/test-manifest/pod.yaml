apiVersion: v1
kind: Pod
metadata:
  name: policy-test
  # This pod being in the default namespace should cause the policy to fail.
  namespace: default
  labels:
    app: myapp
spec:
  containers:
  - image: nginx
    imagePullPolicy: Always
    name: nginx
