apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: insights-webhook-issuer
spec:
  selfSigned: {}
---
# Alternative: Use Let's Encrypt (requires proper DNS setup)
# apiVersion: cert-manager.io/v1
# kind: ClusterIssuer
# metadata:
#   name: insights-webhook-issuer
# spec:
#   acme:
#     server: https://acme-v02.api.letsencrypt.org/directory
#     email: your-email@example.com
#     privateKeySecretRef:
#       name: insights-webhook-issuer-account-key
#     solvers:
#     - dns01:
#         webhook:
#           groupName: acme.yourcompany.com
#           solverName: your-dns-solver
