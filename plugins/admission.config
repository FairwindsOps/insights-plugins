BASEDIR='admission' # Set Base Dir to ci so base.conf can find the version file.
REPOSITORY_NAME=fairwinds/insights-admission-controller
RUN_TEST=false
. `dirname "$BASH_SOURCE"`/base.conf
BASEDIR='.'
DOCKERFILE='admission/Dockerfile'
