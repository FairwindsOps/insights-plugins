BASEDIR='ci' # Set Base Dir to ci so base.conf can find the version file.
REPOSITORY_NAME=fairwinds/insights-ci
RUN_TEST=false
. `dirname "$BASH_SOURCE"`/base.conf
BASEDIR='.' # Set the Base Dir back to . so that we can pull the Trivy code in
DOCKERFILE='ci/Dockerfile'                                                                        
